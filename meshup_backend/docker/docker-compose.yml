version: '3.8'
services:
  # db:
  #   image: postgres:14
  #   environment:
  #     POSTGRES_DB: meshup_db
  #     POSTGRES_USER: meshup_user
  #     POSTGRES_PASSWORD: 1
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
  
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ..:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
    env_file:
      - ../.env

volumes:
  postgres_data:
